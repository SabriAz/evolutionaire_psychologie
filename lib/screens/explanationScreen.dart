import 'package:flutter/material.dart';

class explanationScreen extends StatelessWidget {
  const explanationScreen({super.key});
  @override
  Widget build(BuildContext build) {
    return Scaffold(
      body: Text("Uitleg scherm"),
    );
  }
    
}