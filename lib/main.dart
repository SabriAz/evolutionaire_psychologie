import 'package:flutter/material.dart';
import 'screens/homeScreen.dart';
import 'screens/endScreen.dart';

//adding comment to push branch

void main()
{
  // runApp(myStatefullApp());

  // runApp(HomeScreen());

  runApp(EndScreen());

}
